{
  "private": true,
  "name": "agentlens-monorepo",
  "version": "0.1.0",
  "description": "AgentLens monorepo — DevTools for AI coding-agent context",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run build --workspaces && node --test tests/tokenizer.test.ts tests/similarity.test.ts tests/conflicts.test.ts tests/adapters.test.ts tests/edge.test.ts tests/cli.test.ts",
    "lint": "npm run lint --workspaces",
    "scan": "node packages/cli/dist/index.js scan",
    "benchmark": "node benchmarks/bench.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
